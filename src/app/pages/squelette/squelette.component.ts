import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-squelette',
  standalone: true,
  imports: [
    RouterModule,
  ],
  templateUrl: './squelette.component.html',
  styleUrl: './squelette.component.scss'
})
export class SqueletteComponent {

}
